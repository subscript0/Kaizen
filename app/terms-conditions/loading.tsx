import LegalLoading from '@/components/ui/LegalLoading';

/** /terms-conditions — six numbered clauses in the shared legal grammar. */
export default function Loading() {
  return <LegalLoading clauses={6} />;
}

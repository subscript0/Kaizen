import LegalLoading from '@/components/ui/LegalLoading';

/** /privacy-policy — nine numbered clauses in the shared legal grammar. */
export default function Loading() {
  return <LegalLoading clauses={9} />;
}
